# Foundry-managed git submodule deps (run `forge install` from each subdir to populate)
lib/

# Per-contract puya-sol output (regenerated by compile_all.sh)
out/

# Foundry build artifacts (not used by puya-sol)
artifacts/
cache/
broadcast/

# Python test caches
__pycache__/
.pytest_cache/
*.pyc

# Test output log captures (transient)
test_results_*.log
